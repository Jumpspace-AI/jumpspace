{
  "suite": "source-evidence",
  "shared_candidates": [
    {
      "path": "src/metrics/quarterly-extract.ts",
      "statuses": ["modified"],
      "sources": ["fixture"],
      "content": "export function extractQuarterlyMetrics() { return normalizeQuarterlyMetricRows(); }\n"
    },
    {
      "path": "src/auth/password-entry.tsx",
      "statuses": ["modified"],
      "sources": ["fixture"],
      "content": "export function PasswordEntryForm() { return credentialPasswordEntry(); }\n"
    },
    {
      "path": "src/shared/touched-file.ts",
      "statuses": ["untracked"],
      "sources": ["fixture"],
      "content": "export const touchedFile = true;\n"
    }
  ],
  "cases": [
    {
      "id": "metrics-heading",
      "description": "Metrics heading chooses the metrics file from a shared changed-file pool.",
      "task": {
        "id": "TASK-METRICS",
        "title": "Quarterly metrics extraction",
        "module": "metrics",
        "keywords": ["quarterly", "metrics", "extraction"],
        "spec": "Quarterly metrics extraction normalizes metric rows for reporting."
      },
      "expected": {
        "path": "src/metrics/quarterly-extract.ts",
        "field": "code",
        "max_rank": 1,
        "min_matched_terms": 2
      }
    },
    {
      "id": "password-heading",
      "description": "Password heading chooses the auth file from the same changed-file pool.",
      "task": {
        "id": "TASK-PASSWORD",
        "title": "Password entry form",
        "module": "auth",
        "keywords": ["password", "credential", "entry"],
        "spec": "Users enter credentials through the password entry form."
      },
      "expected": {
        "path": "src/auth/password-entry.tsx",
        "field": "code",
        "max_rank": 1,
        "min_matched_terms": 2
      }
    },
    {
      "id": "unrelated-heading",
      "description": "Unrelated headings get no useful suggestions from the same touched files.",
      "task": {
        "id": "TASK-BILLING",
        "title": "Invoice approval workflow",
        "module": "billing",
        "keywords": ["invoice", "approval"],
        "spec": "Approvers review invoice totals before payment."
      },
      "expected": {
        "path": null,
        "field": null,
        "max_rank": null,
        "min_matched_terms": 0
      }
    }
  ]
}
